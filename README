== Cucumber Examples - Selenium

You need to start the server in the test environment to run the features
./script/server -e test -p 3001

default:  spec/features
spec:     rake spec
features: rake features

